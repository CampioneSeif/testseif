// theme/index.ts
// This file ensures theme is loaded before any component renders
import './config';

// Re-export for convenience
export { Colors, Typography, Spacings, ThemeManager } from 'react-native-ui-lib';
